const webshot = require('webshot');

// https://github.com/brenden/node-webshot
const makeThumbnail = (workData) => {
  // create html for work (SSR)
  // take screenshot
  // save screenshot
  // return screenshot url
};
/*
webshot('google.com', 'google.png', {
  screenSize: {
    width: 320,
    height: 480
  }
},function(err) {
  // screenshot now saved to google.png
});
*/

module.exports = makeThumbnail;
